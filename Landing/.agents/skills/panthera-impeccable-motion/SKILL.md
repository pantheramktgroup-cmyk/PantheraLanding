```md --- name: panthera-impeccable-motion description: Use this skill when modifying Panthera landing UI, CTA buttons, A/B variants, hero sections, hover states, entrance animations, GSAP motion, spacing, typography, layout polish, or visual hierarchy. --- # Panthera Impeccable Motion & Design Skill Use this skill for Panthera landing page design, animation and A/B variant refinement. This project already has GSAP Skills installed. Use those skills for correct GSAP implementation, React cleanup, ScrollTrigger behavior, timelines, performance, and accessibility. This skill defines Panthera’s visual taste, motion direction and A/B variant rules. --- ## Core taste direction The visual standard is: - premium - cinematic - dark - high-ticket - precise - editorial - conversion-focused - elegant but visually memorable Avoid: - generic SaaS UI - cheap arcade effects - excessive gradients - childish bounce - random motion - effects that harm readability - changing layout without need - hardcoding copy outside `landingCopy.js` Design references: - Emil Kowalski-style motion: precise, smooth, intentional microinteractions. - Impeccable design: spacing, hierarchy, alignment, typography, rhythm and polish. - Premium conversion UI: CTAs must attract attention without looking cheap. --- ## General rules 1. Do not touch the thank you page unless explicitly requested. 2. Preserve the A/B testing system. 3. Keep copy centralized in `src/content/landingCopy.js` whenever possible. 4. Do not duplicate the whole landing. 5. Use clean conditional rendering for `variant === 'A'` and `variant === 'B'`. 6. Respect `prefers-reduced-motion`. 7. Do not leave unused imports or dead code. 8. Run `npm run build` after changes. 9. Do not apply variant-specific changes globally. 10. Keep desktop and mobile polished. --- ## CTA button rules All main CTA buttons must say: `AGENDAR DIAGNÓSTICO` The navbar CTA is an exception in size only: - it must stay compact - it must fit inside the navbar - it must not inherit the large CTA dimensions - it may share hover behavior, but not large CTA sizing --- ## Button Variant A Use this for landing Variant A. Visual behavior: - fixed Panthera yellow/green background - black text - no full light sweep across the button - no white shine crossing the full button fill - animated border glow travelling around the perimeter - the glow should feel like it is orbiting the border - hover can turn background white - text remains black on hover - hover should feel sharp, clean and premium Entrance animation: - button enters visibly - use a strong falling or dropping motion - slight overshoot - controlled bounce - duration around 0.9s to 1.3s - ease like `back.out(1.8)` or equivalent - after entrance, optional subtle attention pulse Do not make it arcade or childish. --- ## Button Variant B Use this for landing Variant B. Visual behavior: - still premium and Panthera-branded - more experimental than Variant A - stronger motion language - hover can use magnetic pull, elastic press, diagonal energy, or antigravity feel - background may turn white on hover - text remains black - avoid covering the entire button with a white sweep unless explicitly requested - keep readability perfect Entrance animation: - more antigravity / floating impact - button may rise/fall into place - use scale from 0.82 to 1 - slight rotation correction is allowed - controlled rebound - should be very visible - more daring than Variant A, but still premium --- ## Suggested animation patterns Use CSS and/or GSAP. Recommended class names: - `btn-orbit-border` - `btn-impact-enter-a` - `btn-impact-enter-b` - `btn-premium-hover` - `btn-magnetic-hover` - `btn-antigravity-enter` - `btn-attention-pulse` Use GSAP Skills for: - timelines - ScrollTrigger - React cleanup - performance - reduced motion - staggered entrances - hover/interaction timelines --- ## Hero rules When modifying the Hero: - do not destroy the current composition - adjust spacing carefully - if Variant B feels too low, move only the text block upward - if titles feel too large, reduce font sizes slightly with clamp values - keep both variants visually related but not identical - protect mobile layout --- ## Line break rules When fixing line breaks: 1. First check `landingCopy.js` for `\n`. 2. Then check whether the component uses `whitespace-pre-line`. 3. Then adjust `max-width`. 4. Then adjust font size. 5. Avoid solving line breaks only by adding random `<br />` in components. 6. Do not let narrow max-width create accidental extra lines. 7. For important headlines, manual line breaks should define the composition. --- ## Card alignment rules When fixing cards: - all cards in the same section must align content vertically - if needed, define a fixed internal content start point - titles and descriptions should begin at the same visual height - hover states must not break alignment - if a section uses one image distributed across multiple cards, do not repeat the same image in every card - preserve the intended grid composition --- ## A/B variant discipline Never apply a variant-specific change globally. For buttons: - Variant A uses orbiting border glow. - Variant B uses stronger magnetic / antigravity motion. For layouts: - if the user asks for a Variant A-only change, do not modify Variant B. - if the user asks for a Variant B-only change, do not modify Variant A. - shared fixes apply only when explicitly described as general. --- ## QA requirements After changes: - test `/?variant=A` - test `/?variant=B` - test desktop - test mobile - run `npm run build` - fix unused imports - remove dead code ``` Después de crearla, usá este prompt único: ```txt Usá las GSAP Skills instaladas y la skill local `panthera-impeccable-motion`. Aplicá los cambios de Hero y botones respetando la disciplina A/B: 1. En ambas variantes, achicar un poco el tamaño del título principal del Hero. 2. En Variante B, subir el bloque de texto del Hero para que el título empiece más arriba. 3. En Variante A, el botón debe volver al efecto original: fondo amarillo/verde fijo, texto negro, sin barrido blanco interno, solo borde brillante orbitando el perímetro. Hover con fondo blanco y texto negro. 4. En Variante B, crear un efecto distinto: más magnético / antigravity, con entrada más atrevida, hover con fondo blanco y texto negro. 5. El botón del navbar debe seguir compacto y no heredar el tamaño de los botones grandes. 6. Las animaciones de entrada de botones deben ser mucho más visibles: caída, llegada, rebote u overshoot real. 7. Respetar prefers-reduced-motion. 8. No tocar la thank you page. 9. Mantener funcionando `/?variant=A` y `/?variant=B`. 10. Ejecutar `npm run build` y corregir errores. ``` Ese sería el camino más limpio: **GSAP Skills = técnica**, **Panthera skill = criterio visual y reglas A/B**.